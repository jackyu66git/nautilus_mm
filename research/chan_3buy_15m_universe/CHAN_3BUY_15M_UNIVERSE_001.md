# CHAN_3BUY_15M_UNIVERSE_001

```
Track:     先修母集。不是 EMA
Phase:     冻结三买定义，15m 与 1H 各自独立重识别
Input:     同一 90 天 1m 窗口。定义 = CHAN_3RD_POINT_001 的 detect，不准改
```

不加 EMA / OF。不优化。不改结构算法。

只问：

```
1H 独立 B3/S3  = ?
15m 独立 B3/S3 = ?
15m 重识别 是否等于 冻结的 20 个 event_id？
```

本枪不测命运、不测均线。`CHAN_3BUY_EMA52_001` 的 9 vs 11 不准改。
