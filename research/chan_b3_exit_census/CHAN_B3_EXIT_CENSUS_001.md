# CHAN_B3_EXIT_CENSUS_001

```
Track:     Exit Geometry。接 CHAN_B3_BASELINE_V1 TIME_DOMINATED
Phase:     描述性 Census。不是 V2。不是优化。不算收益
Input:     冻结 20 个 15m B3/S3。同一 Entry / Stop / 窗口
```

只问：确认后下一根开盘进场，在盒沿 Stop 之前，价格自然能走多远、要多久。

不改：B3/S3 定义、15m、T1 open、zd/zg Stop、窗口。无 EMA / OF / ATR / 手续费。
V1 的 −0.58R **不是** Economic FAIL。

## 预注册

R 网格（不分桶搜索）：`0.25 0.5 0.75 1 1.25 1.5 2`
时钟（不是选最优 Time）：`24h 48h 72h`，以及扫到 Stop 或样本末。

同根既触目标又触 Stop → 与 V1 一致，计 **Stop first**。

## 判定（本枪结束就分支，不再开解释 Census）

```
hit24(r)  = 24h 内、Stop 之前到达 r
hit_any(r)= 在 Stop 或样本末之前到达 r

A  较小目标更自然
   hit24(0.5) ≥ 0.40 且 hit24(0.5)−hit24(1) ≥ 0.20
   → 下一授权 V2：测 0.5 / 0.75 / 1R + OOS

B  目标能到，24h 太短
   非 A，且 hit_any(1)−hit24(1) ≥ 0.20
   → 下一授权验证 24 / 48 / 72h

C  小目标也没有明显改善
   → 停三买交易线。才允许回头看 Entry
   不准改加 EMA / OF / Trend Age
```

A/B 都不自动开下一枪。C 关闭本线。

## 冻结结果

`PASS / A_SMALLER_TARGET`。n=20。V1 −0.58R 不是 Economic FAIL。

24h 到达率（Stop 之前）：

| R | hit@24 | stop@24 | none | 到达 p50 |
|---|--------|---------|------|----------|
| 0.25 | 15/20=0.75 | 2 | 3 | 3.25h |
| 0.5 | **11/20=0.55** | 3 | 6 | **4.25h** |
| 0.75 | 6/20=0.30 | 4 | 10 | 3.5h |
| 1.0 | 4/20=0.20 | 4 | 12 | 1.25h |
| 2.0 | 2/20=0.10 | 4 | 14 | 2.25h |

判定：hit24(0.5)=0.55≥0.40 且 0.55−0.20=0.35≥0.20 → **A**。

1R 不是做不到：hit_any(1)=10/20=0.50，到达 p50=24.5h（正好卡在 24h 截断上）。@48h=8/20，@72h=9/20。这是 A 之外的时间信息，不另开 Time Census。
2R 仍差：any 5/20。
B3 0.5@24=8/12；S3=3/8。分记，不合桶当 Alpha。
mfe_24 p50=0.52R（复现 V1）。Stop 最终 16/20 会碰到，p50=64h。

下一授权（不自动开）：`CHAN_B3_V2` 测 0.5 / 0.75 / 1R。同一 20 笔上的 PnL 只是 IS，不能当选参。OOS 必须另立 Validation Clock。不准加 0.25R，不准加 EMA/OF。

## 冻结结果

`PASS / A_SMALLER_TARGET`。n=20。V1 −0.58R 不是 Economic FAIL。

24h 到达率（Stop 之前）：

| R | hit | stop first | none | 到达 p50 |
|---|-----|------------|------|----------|
| 0.25 | 15/20=0.75 | 2 | 3 | 3.25h |
| 0.50 | 11/20=0.55 | 3 | 6 | 4.25h |
| 0.75 | 6/20=0.30 | 4 | 10 | 3.5h |
| 1.00 | 4/20=0.20 | 4 | 12 | 1.25h |
| 2.00 | 2/20=0.10 | 4 | 14 | 2.25h |

1R 拉长时间：24h 4/20 → 48h 8/20 → 72h 9/20 → 至 Stop 10/20（到达 p50=24.5h）。
0.5R 几乎不靠加时：24h 11/20 → any 12/20。
mfe_24/48/72 p50 = 0.52 / 0.60 / 0.70 R。B3 0.5R@24 = 8/12；S3 = 3/8。

A 成立（0.5R@24=0.55，比 1R 高 35pp）。1R 也有「24h 太短」的成分，但主结论是较小目标更自然。
下一授权才是 `CHAN_B3_V2`：测 0.5 / 0.75 / 1R。同一 20 笔上的 PnL 只是 IS，不能当选参。OOS 另立时钟。
不准加 EMA/OF。不准把 0.25R 当 V2（费后会更碎）。

## 禁止
