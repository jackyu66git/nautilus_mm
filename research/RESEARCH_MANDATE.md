# Research Mandate

默认治理 = **研究委托制**。不再逐 Phase 找人按继续。

```
授权一条 Research Track
        ↓
定义 → 审计 → Replay → 分析 → PASS 继续 / FAIL 关闭
        ↓
仅跨轨时询问
```

当前状态：**沿 `CHAN_TO_LIVE.md` 走。Active = G0 WAIT。**
`CHAN_B3_R_CENSUS_001` = NO_TAIL / SHAPE_OK。G0b = N/A。
等自然 S3 至 n_S3≥16 才允许一次 `CHAN_B3_V2_RECHECK`。等是合同，不是停工。
不准一枪完再规划。不准 Cap Stop。不准另定 5%/10%/15%。
不准新开结构研究。不准只做 B3。不准加 EMA/OF。不准偷看 n_S3<16。

本树已停。连续排除的是伪 Alpha 路径，不是「没东西可做」：

```
Structure
   ├── Fate           PASS · 短期结构确认现象 · B1/B2/B3 HOLD
   ├── Continuation   ONE-BAR ARTIFACT · STOP
   └── Location       HOLD_NO_SPLIT
                      中枢内/外不是独立变量
```

EMA52 FROZEN · Trend Age 仅 Attribution · OF LOCKED OUT
不扩窗、不改定义、不人为分桶。

禁止研究漂移：没有 Location split → 换位置定义 → 再切桶 → 找 split。
禁止继续问「什么条件能让三买更好」。
禁止在本树上继续挖 Fate / Continuation / Location / EMA。

已另立：`CHAN_PENETRATION_CENSUS_001` = PASS / HIERARCHY_CANDIDATE。问 Invalidation，不是「会不会继续」。
假设「多数很浅 / 极少数破坏」不成立。B1 14/18 穿一类；B3 16/20 穿远盒沿。
MAE 扫到样本末（p50=175.5h）不是局部失效深度。B1/B2 盒沿不是反向锚。
本枪停。第二闸另授权。不准 ATR、不准止损、不准收益率。

冻结：
- WHERE = NO_STATE_CONTRAST
- STATE = SAMPLE_INSUFFICIENT
- 3BUY_EMA52 = LOCATION DIAGNOSTIC only（9 vs 11 不准当 Fate Contrast）
- UNIVERSE = 15m 独立 20 = 冻结母集。不必再跑
- 15m = 候选主周期 PASS。EMA52 放下

`CHAN_3BUY_EMA52_001` 仅 LOCATION DIAGNOSTIC，不能当 15m 样本扩张结论。9 vs 11 不准改。

`CHAN_EMA52_STATE_001` = SAMPLE_INSUFFICIENT（停）。`CHAN_EMA52_WHERE_001` 已封存。
`CHAN_3RD_POINT_001` Phase 2 仍暂停。

## 自动推进

主线：`CHAN_TO_LIVE.md`。G-R 已结束 = NO_TAIL。下一动作只有等 n_S3≥16 后的 `CHAN_B3_V2_RECHECK`。
不准再开会规划。不准为「有事情做」立项。不准新开结构研究、Penetration 第二闸、B1/B2 交易线。
不准问「什么条件让三买更好」。不准用 EMA/OF 救 S3。不准把结构 Stop 改近。

另开实验仅当：全新机制假设、另立研究线（不改本树），已跨轨询问，且同时满足：

- 不改 Fact Tape、不改 1m 源数据窗口
- 不改已关闭实验的合同与数字
- 不跨研究层（结构 → SMC / OF / 交易）
- 不换周期（15m/1H）
- 不把结果收成交易 Setup

## 自动停止并询问

- 数据泄漏
- 定义无法在线实现
- 有效样本不足且无法在本 Track 换切面补
- 证据互相矛盾
- 必须改已冻结对象
- 换周期
- 进入新研究方向、SMC、OF、Trading Layer
- 再挖一类/二类/三类买卖点，或开第四种 BSP
- 把 BROKE_FIRST 算作二买失败
- 把二买失效锚从一类高低改成中枢盒子
- 未定义清「非结构对照」就开对照实验
- 把弱 RESUME 与 EMA52 / OF / Trend Age 叠加当成 Alpha
- 用 29/47 Fate 代替 27/47 纯 continuation 做对照比较
- 把三类合桶写成结构 Alpha
- 改 H 或改成「相对 T0 仍新高」来否定 ONE_BAR_ARTIFACT
- 把 OUT 再切 ATR / 把 OUT_HIGH vs OUT_LOW 当 Location Modifier
- 基础 Location 无对照时改开 EMA52
- 换一个位置定义再切桶寻找 split
- 在本树上继续问「什么条件能让三买更好」
- 把 0.5/1 ATR 写成结构失效
- 把穿透普查做成止损优化或收益率
- 用 Fate / Continuation 数字回调穿透层
- 为对齐穿透率而改一类/二类/三类锚
- 改 CHAN_B3_BASELINE_V1 的 Time / TP / Stop / 1R
- 把 BASELINE_OK 写成 Economic PASS 或实盘资格
- 把 Exit Census 的 0.25R 加成 V2 目标
- 在同一 20 笔上把 V2 PnL 写成 OOS
- 把 V2 收成只做 B3 以绕开 FAMILY_SPLIT
- n_S3<16 时偷看复检或加指标救 S3
- 在 HOLD 期间新开结构研究 / B1/B2 交易线 / Penetration 第二闸
- 把结构 Stop Cap 近，或用 5%/10%/15% 当未普查的交易阈值
- Gate 未给出判定时重新规划或临时加实验
- 在官方 NautilusTrader / freqtrade 上游仓库改本研究合同

## 自动失败关闭

`NO_INCREMENT / NO_GRADIENT / NO_STATE_CONTRAST / NO_FATE_CONTRAST`  
→ 该局部假设关闭，不准再榨，不必询问。

## Replay

合同合法且未触发停止条件 → 自动 Replay。不再使用「合同冻、Replay BLOCKED、等人授权」。
