# CHAN_BI_OF_STRATA_001

```
Experiment:     CHAN_BI_OF_STRATA_001
Replay:         DONE
decision:       CLOSED / NO_GRADIENT
kind:           NO_GRADIENT
S0 PASS · S1 PASS · S2 FAIL · S3 NOT_RUN
结论范围：1m Binance Kline taker-flow 代理，
在确认笔端点之后没有发现独立的价格路径增量。
不能升级成「OF 无效」。
```

核心（结构 × 微观质量归因，不是找预测器）：

```
几何分型
   ↓
确认成为笔端点
   ↓
T_BI_SURE                 ← 经济对象从这里才成立
   │
   └── 回看 T_FX_VISIBLE 之前的 OF
              ↓
         OF 五分位
              ↓
       MFE / MAE / 持有收益
```

禁止 look-ahead：不能因为事后知道是笔端点，就把整个形成过程都当成端点来统计。

缠论回答「这里是不是结构转折」。OF 回答「这个转折的微观质量如何」。能不能交易，等五分位结果再说。

```
缠论
  │
  └── 几何分型 → 确认笔端点     ← 结构对象
                    │
                    └── OF 排序   ← 微观质量分（研究发现，不做成阈值）
                          │
                          ▼
                    分桶经济统计
```

缠论找转折对象。OF 不找点，只给这个对象打分。

---

## 两组（同一对象）

```
A  全部确认笔端点反转
B  同样端点，按 forming OF 五分位分层
```

桶（研究分位，不是交易阈值）：

```
全部确认端点
├── OF 最弱 20%
├── 20–40%
├── 40–60%
├── 60–80%
└── OF 最强 20%
```

底端点：OF 更空 = 更强（−delta 更高分）。顶端点对称。先报底。

---

## 钟（不许混）

| 钟 | 定义 | 用途 |
|----|------|------|
| `T_FX_VISIBLE` | 端点那根分型第一次确认 | OF 窗口右界 |
| OF score | forming，`t < T_FX_VISIBLE` | 只排序。与 CHAN_FX_BI_OF_001 相同 |
| `T_BI_SURE` | 该端点所在笔 `is_sure` 的确认时刻 | **经济路径起点**。此时才知道是笔端点 |
| 前瞻 | `t > T_BI_SURE` 的 15m 价格 | MFE / MAE / 持有收益 |

禁止：在 `T_FX_VISIBLE` 当笔端点入场（当时还不知道会成笔）。  
禁止：用 `T_BI_SURE` 之后的 OF 改分数。

---

## 只记这些（预指定，不挑最好的 N）

每个桶、底端点：

- 后续最大有利波动 MFE
- 后续最大不利波动 MAE
- 固定持有 4 / 8 / 16 根 15m 的收益（1h / 2h / 4h）
- 反转后收益分布（p10 / p50 / p90）
- 手续费后 expectancy：毛收益 − 2×taker 4bps（预指定，不调费率猎金）

A 是全样本对照。真正有意义的是 **Q1→Q2→Q3→Q4→Q5 的梯度**，不是「Q1 赚钱、Q5 赚钱所以有效」。

优先看 **MAE 是否随 OF 质量改善**。那回答的是：转折结构在成交层有没有不同质量，不是预测涨跌。

---

## 五种结果（一等，不互相改写，都不需要 B1/B2 / HTF / SMC）

| 结果 | 含义 | 下一步 |
|------|------|--------|
| 完全没有梯度 | OF 对端点后价格没有独立价值 | 关闭质量分经济链 |
| 只有 MFE 有梯度，MAE 没有 | 可能描述后续推动，不一定改善风险 | 记实。不做成入场器 |
| 只有短持有有梯度，16 根消失 | OF 是微观短期信息 | 记实。不准拉长周期猎金 |
| 费用后全部消失 | 有市场信息，不足以形成经济 Edge | 关闭可交易声称 |
| Q1→Q5 稳定单调 | 才值得进入下一层 | 本 ID 通过后才准设计下一层 |

不准提前设计下一层。Replay 保持 BLOCKED。

---

## SMOKE-60D-15M

Log: `logs/chan_bi_of_strata/CHAN_BI_OF_STRATA_001/SMOKE-60D-15M/`

```
S0 PASS   leak=0  T_BI_SURE>T_FX_VISIBLE  n_ep=270  bottom=135  per_q=27×5
S1 PASS
S2 FAIL   NO_GRADIENT
          MAE Q1→Q5 med  0.00478 0.00371 0.00396 0.00366 0.00482   ρ=+0.10
          MFE Q1→Q5 med  0.00467 0.00273 0.00526 0.00418 0.00247   ρ=−0.50
          ret_8 ρ=−0.90（与「质量越好越赚钱」相反，不当成新假设）
S3 NOT_RUN
decision  FAIL
kind      NO_GRADIENT
```

MAE 没有随 OF 质量改善。没有 Q1→Q5 稳定梯度。  
FAIL = OF 对确认笔端点之后的价格路径没有独立经济价值。  
不接 B1/B2 / HTF / SMC，不换 TF，不做成 classifier 救。

---

## 门

| 门 | FAIL |
|----|------|
| S0 Clock | 经济路径用了 `T_BI_SURE` 之前当已确认端点，或 OF 用了 `t >= T_FX_VISIBLE` |
| S1 Size | 确认底端点不够五分位（每桶太稀） |
| S2 Monotone | OF 分位与经济量无单调关系（乱序 / 只有两端偶然好看） |
| S3 Amplitude | 单调性能被端点振幅单独解释 |

S2 看的是整条 Q1→Q5 梯度。两端碰巧赚钱而中间乱序 = FAIL。  
尤其报 MAE 梯度。全过 ≠ 策略。不准用 B1/B2 / HTF / SMC 解释任何一种结果。

禁止：HTF、SMC、B1/B2、换 TF、阈值猎金、把 OF 当端点识别器。
